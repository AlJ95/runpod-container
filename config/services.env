# Services Configuration
# This file contains environment variables for all services

# Audio Service (Faster-Whisper)
AUDIO_SERVICE_HOST="${AUDIO_SERVICE_HOST:-0.0.0.0}"
AUDIO_SERVICE_PORT="${AUDIO_SERVICE_PORT:-6000}"
SST_MODEL="${SST_MODEL:-large-v3}"
WHISPER_COMPUTE_TYPE="${WHISPER_COMPUTE_TYPE:-int8}"

# TTS Service (VibeVoice)
TTS_SERVICE_HOST="${TTS_SERVICE_HOST:-0.0.0.0}"
TTS_SERVICE_PORT="${TTS_SERVICE_PORT:-5000}"
TTS_MODEL="${TTS_MODEL:-aoi-ot/VibeVoice-7B}"
LANGUAGE="${LANGUAGE:-en}"
VIBEVOICE_VOICES_DIR="${VIBEVOICE_VOICES_DIR:-voices}"

# Common settings
DEBUG_MODE="${DEBUG_MODE:-false}"
LOG_LEVEL="${LOG_LEVEL:-info}"

# Resource management
MAX_CONCURRENT_REQUESTS="${MAX_CONCURRENT_REQUESTS:-4}"
REQUEST_TIMEOUT="${REQUEST_TIMEOUT:-300}"

# API endpoints
VLLM_API_BASE_URL="${VLLM_API_BASE_URL:-http://localhost:8000}"
AUDIO_API_BASE_URL="${AUDIO_API_BASE_URL:-http://localhost:6000}"
