# CosyVoice runtime requirements (filtered from upstream)
#
# Purpose:
# - Install CosyVoice inference dependencies without forcing torch/torchaudio versions.
# - Avoid heavy/optional training/runtime packages (e.g. deepspeed, tensorrt).
#
# NOTE:
# CosyVoice upstream pins torch==2.3.1 / torchaudio==2.3.1, but vLLM 0.10.2
# requires torchaudio==2.8.0. We therefore intentionally do NOT pin torch/torchaudio
# here and let the unified environment resolve a compatible set.

conformer==0.3.2

diffusers==0.29.0

gdown==5.1.0
grpcio==1.57.0
grpcio-tools==1.57.0

hydra-core==1.3.2
HyperPyYAML==1.2.2
inflect==7.3.1

librosa==0.10.2
lightning==2.2.4
matplotlib==3.7.5
modelscope==1.20.0
networkx==3.1
numpy==1.26.4
omegaconf==2.3.0

onnx==1.16.0
onnxruntime-gpu==1.18.0; sys_platform == 'linux'

# openai-whisper installed separately with --no-deps (see scripts/setup.sh)
protobuf==4.25
pyarrow==18.1.0
# Do not pin pydantic here; vLLM requires newer versions (>=2.11.x)
pyworld==0.3.4
rich==13.7.1
soundfile==0.12.1
tensorboard==2.14.0

# Do not pin transformers here; main requirements already constrain it
x-transformers==2.11.24

uvicorn==0.30.0
wetext==0.0.4
wget==3.2
